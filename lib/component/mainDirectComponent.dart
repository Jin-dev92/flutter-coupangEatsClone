import 'package:flutter/material.dart';

class mainDirectComponent extends StatefulWidget {
  const mainDirectComponent({Key? key}) : super(key: key);

  @override
  _mainDirectComponentState createState() => _mainDirectComponentState();
}

class _mainDirectComponentState extends State<mainDirectComponent> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:
      Text("mainDirectCompoent")
    );
  }
}
