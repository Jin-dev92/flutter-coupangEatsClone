import 'package:flutter/material.dart';

class Favorite extends StatelessWidget {
  const Favorite({Key? key}) : super(key: key); // 무슨 코드인지 확인 해 볼 것.
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("즐겨찾기"),
    );
  }
}